#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Prints out single digits number of base ten
 * Return: Always (Success)
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
